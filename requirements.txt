# Core ML and Audio Processing
tensorflow==2.13.0
keras==0.0.0
numpy==1.24.3
librosa
soundfile

# Flask Web Server
flask
flask-cors

# Optional: For audio conversion or debugging
scipy
matplotlib

# Optional: Only needed if you work with MP3 or non-WAV formats
pydub